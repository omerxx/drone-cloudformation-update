pipeline:
  docker:
    image: plugins/docker
    repo: omerxx/drone-cloudformation-update
    tags: 
      - ${DRONE_BUILD_NUMBER}
      - drove
    secrets:
      - docker_username
      - docker_password

