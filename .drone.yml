pipeline:
  docker:
    image: plugins/docker
    repo: omerxx/drone-cloudformation-update
    tags: 
      - ${DRONE_BUILD_NUMBER}
      - spotim
    secrets:
      - docker_username
      - docker_password

